  <h1>Nos nains</h1>
  <h2>Heyy c'est mout</h2>

  <ul>
    <% @dwarves.each do |dwarf| %>
      <li>
          <%= link_to dwarf.name, dwarf_path(dwarf) %>
      </li>
    <% end %>
  </ul>

<%= link_to "Add a dwarf", new_dwarf_path %>


<p>Ceci est ma branche</p>
